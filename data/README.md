# Dataset

This portfolio repository does not redistribute the university-provided dataset.

To run the project, place an authorised copy of the heat-safety CSV here and name it:

```text
heat_safety_dataset.csv
```

The training script expects the file at `data/heat_safety_dataset.csv`.
